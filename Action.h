#ifndef __ACTION_H__
#define __ACTION_H__
#include "Actor.h"


/*struct Action
{
	Action( Actor *a, const std::string &actionName );
	void Update();
	void SetToFrame( int frame );
	
	sf::Uint32 lastFrame;
	sf::Uint32 currentFrame;
	bool isActive;
	std::string name;
	sf::Vector2f startSprite;
	sf::Vector2f spriteSize;
	sf::Uint32 hitboxStartFrame;
	sf::Uint32 hitboxLastFrame;
	//DynamicObject *hitbox;
	//DynamicObject *body;
	Actor *actor;
	std::list<Actor*> m_actorsCollided;

};*/



#endif